'use strict';

var React = require('react-native');
var {Text, TouchableHighlight, TextInput} = React;

var LoginPage = React.createClass({
    render: function() {
        return (<Text>Here is Search Page</Text>);
    }
});

module.exports = LoginPage;
