'use strict';

var React = require('react-native');
var {Text, TouchableHighlight} = React;

var PinnedEvents = React.createClass({
    render: function() {
        return (<Text>Here are Pinned Events</Text>);
    }
});

module.exports = PinnedEvents;
