'use strict';

var React = require('react-native');
var {Text, TouchableHighlight} = React;

var UserProfile = React.createClass({
    render: function() {
        return (<Text>Here is User Profile</Text>);
    }
});

module.exports = UserProfile;
