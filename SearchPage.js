'use strict';

var React = require('react-native');
var {Text, TouchableHighlight} = React;

var SearchPage = React.createClass({
    render: function() {
        return (<Text>Here is Search Page</Text>);
    }
});

module.exports = SearchPage;
